const Node = class {
  constructor(name, value) {
    this.name = name,
    this.value = value,
    this.neighbors = []
  }

  addNeighbors() {
    let neighbors = this.neighbors;

    return this.neighbors;
  }

}


module.exports = Node;
